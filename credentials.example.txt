[github]
token=github-token

[remote.ssh]
host=server-ip
port=22
user=deploy-user
password=ssh-password
root_password=root-or-sudo-password

# Blog deployment reuses the ignored credentials.txt files in the three
# sibling projects. Never copy real secrets into this example or into Git.
